package com.example.rockharzplaner.ui.theme

import androidx.compose.ui.graphics.Color

// --- DUNKELMODUS FARBEN (Schwarz & Deep Forest) ---
val GreenDarkBg = Color(0xFF0A0F0A)      // Fast schwarzer Hintergrund mit leicht grünem Einschlag
val GreenDarkCard = Color(0xFF141F14)    // Dunkles Waldgrün für die Bandkarten
val GreenDarkHeader = Color(0xFF1C2D1C)  // Etwas helleres Moosgrün für die Einklapp-Balken
val GreenNeonAccent = Color(0xFF4CAF50)  // Kräftiges Signalgrün für Bühnen, Highlights und Pausen (sehr gut lesbar auf Schwarz)

// --- HELLMODUS FARBEN (Light Forest) ---
val GreenLightBg = Color(0xFFF2F7F2)     // Sehr helles, sanftes Salbeigrün für den App-Hintergrund
val GreenLightCard = Color(0xFFFFFFFF)   // Reinweiß für die Musikkarten (bester Kontrast)
val GreenLightHeader = Color(0xFFD0E1D0) // Dezentes Hellgrün für die Einklapp-Balken
val GreenDeepAccent = Color(0xFF1B5E20)  // Tiefes, dunkles Waldgrün für Bühnen und Akzente im Hellmodus